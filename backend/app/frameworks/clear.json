{
  "id": "CLEAR",
  "name": "CLEAR",
  "description": "Context, Length, Emotion, Audience, Role — great for marketing and communication.",
  "fields": {
    "Context": {"hint":"Background information", "required": true},
    "Length": {"hint":"How long should the output be", "required": false},
    "Emotion": {"hint":"Tone or emotional style", "required": false},
    "Audience": {"hint":"Who is this for", "required": true},
    "Role": {"hint":"What perspective to take", "required": false}
  },
  "template": "Given {Context}, write for {Audience} with a {Emotion} tone. Keep it {Length}. Take the role of {Role}."
}
